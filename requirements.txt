pandas>=2.0
scikit-learn>=1.3             # baselines + TF-IDF retrieval
scipy>=1.11                   # paired t-test for retrieval ablation deltas
pytest>=8.0
python-dotenv>=1.0
kagglehub>=0.2                # scripts/fetch_raw_data.py — raw dump download
groq>=0.11                    # LLM drafts/judge when GROQ_API_KEY is set
# sentence-transformers>=2.2  # deferred: Phase 5 uses TF-IDF (see DECISION_LOG)
# faiss-cpu>=1.7              # deferred with dense embeddings
